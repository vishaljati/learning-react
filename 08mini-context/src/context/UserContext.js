import React from 'react'

const UserContext = React.createContext()

export default UserContext;

/* Note: UserContext is a global variable type where multiple data
cam be rendered in any component*/